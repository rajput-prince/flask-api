# Flask API

This is a Flask API for uploading files.

## Setup

1. Clone the repository
2. Install dependencies: `pip install -r requirements.txt`
3. Run the app: `python app.py`

## Deployment

Deployed on Render. The app will be accessible at `https://your-render-url.com`.
